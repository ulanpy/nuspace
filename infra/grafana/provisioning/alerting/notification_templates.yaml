apiVersion: 1

templates:
  - orgId: 1
    name: nuspace
    template: |
      {{ define "nuspace.telegram.message" }}
      {{ if eq .Status "firing" }}🔴 Alert firing{{ else }}✅ Alert resolved{{ end }}
      {{ end }}
