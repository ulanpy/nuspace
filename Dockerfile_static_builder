# Updated Dockerfile_static_builder
FROM node:20-alpine
WORKDIR /app

# Copy ONLY frontend package files
COPY ./frontend/package*.json ./

# Install production dependencies
RUN npm install --omit=dev

# Copy ONLY frontend source code
COPY ./frontend .

# Build the project
RUN npm run build