# Production static build Dockerfile for Next.js
# Builds static files to /app/out directory
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY frontend/package*.json ./

# Install dependencies
RUN npm install

# Copy source code
COPY frontend .

# Note: Build runs at container runtime via docker-compose command
# This allows the volume mount to capture the output in ../frontend/out


# ============================================================================
# SSR Alternative: Uncomment below for standalone server deployment
# ============================================================================
# # syntax=docker.io/docker/dockerfile:1
#
# # Install dependencies only when needed
# FROM node:20-alpine AS deps
# RUN apk add --no-cache libc6-compat
# WORKDIR /app
#
# COPY frontend/package.json frontend/package-lock.json* ./
# RUN npm ci
#
# # Rebuild the source code only when needed
# FROM node:20-alpine AS builder
# WORKDIR /app
# COPY --from=deps /app/node_modules ./node_modules
# COPY frontend .
#
# # Build Next.js with standalone output
# RUN npm run build
#
# # Production image, copy standalone output and run next
# FROM node:20-alpine AS runner
# WORKDIR /app
#
# ENV NODE_ENV=production
#
# RUN addgroup --system --gid 1001 nodejs
# RUN adduser --system --uid 1001 nextjs
#
# COPY --from=builder /app/public ./public
#
# # Leverage Next.js standalone output
# COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
# COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
#
# USER nextjs
#
# EXPOSE 3000
#
# ENV PORT=3000
# ENV HOSTNAME="0.0.0.0"
#
# CMD ["node", "server.js"]
