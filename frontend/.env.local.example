# Next.js Environment Variables
# Copy this file to .env.local and fill in the values

# =============================================================================
# MIGRATION GUIDE: Vite to Next.js Environment Variables
# =============================================================================
# 
# Vite uses `VITE_` prefix for client-side variables accessed via `import.meta.env`
# Next.js uses `NEXT_PUBLIC_` prefix for client-side variables accessed via `process.env`
#
# Migration mapping:
#   VITE_BASE_URL        -> NEXT_PUBLIC_BASE_URL
#   VITE_ENABLE_SNOWFALL -> NEXT_PUBLIC_ENABLE_SNOWFALL
#   VITE_API_URL         -> NEXT_PUBLIC_API_URL (if used)
#   VITE_GCS_BUCKET      -> NEXT_PUBLIC_GCS_BUCKET (if used)
#
# Code changes required:
#   import.meta.env.VITE_*  -> process.env.NEXT_PUBLIC_*
#
# =============================================================================

# Base URL for the application (used for HMR/WebSocket in development)
# In Docker development, this is automatically set by the start script
NEXT_PUBLIC_BASE_URL=http://localhost:5173

# Enable/disable seasonal snowfall animation (default: true)
# Set to "false" to disable
NEXT_PUBLIC_ENABLE_SNOWFALL=true

# Google Cloud Storage bucket name (if applicable)
# NEXT_PUBLIC_GCS_BUCKET=your-bucket-name

# API URL (typically not needed as /api is proxied by nginx)
# NEXT_PUBLIC_API_URL=/api
